import numpy as np
from spotify_dataset import *


Importances = np.array([[0.1715753504265557 for i in range(SpotifyDataset.SESSION_PREDICTABLE_FEATURES)],
                        [0.14228596666589177 for i in range(SpotifyDataset.SESSION_PREDICTABLE_FEATURES)],
                        [0.12275971082544924 for i in range(SpotifyDataset.SESSION_PREDICTABLE_FEATURES)],
                        [0.10811501894511735 for i in range(SpotifyDataset.SESSION_PREDICTABLE_FEATURES)],
                        [0.09639926544085173 for i in range(SpotifyDataset.SESSION_PREDICTABLE_FEATURES)],
                        [0.08663613752063046 for i in range(SpotifyDataset.SESSION_PREDICTABLE_FEATURES)],
                        [0.07826774216044068 for i in range(SpotifyDataset.SESSION_PREDICTABLE_FEATURES)],
                        [0.07094539622027474 for i in range(SpotifyDataset.SESSION_PREDICTABLE_FEATURES)],
                        [0.06443664427346056 for i in range(SpotifyDataset.SESSION_PREDICTABLE_FEATURES)],
                        [0.05857876752132777 for i in range(SpotifyDataset.SESSION_PREDICTABLE_FEATURES)]], dtype=np.float32)
